package feathers.controls;
@:build(core.utils.CoreClassMacro.buildFields("gui.pop.up.list.view"))

class GuiPopUpListview extends PopUpListView{
    public function new(){

        super();
        core.utils.CoreClassMacro.construct();
    }
}